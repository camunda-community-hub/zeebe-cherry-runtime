# Upload Directory

Place JAR files here to have them loaded automatically at startup.

Cherry scans this directory on startup and uploads every JAR it finds into the connector storage. This is a quick way to provision connectors without going through the UI or the Store.

Files in this directory are processed once at startup; they are not watched for changes at runtime.
